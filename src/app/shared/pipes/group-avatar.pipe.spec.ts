import { GroupAvatarPipe } from './group-avatar.pipe';

describe('GroupAvatarPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupAvatarPipe();
    expect(pipe).toBeTruthy();
  });
});
